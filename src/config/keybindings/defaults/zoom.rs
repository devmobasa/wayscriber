pub(crate) fn default_toggle_frozen_mode() -> Vec<String> {
    vec!["Ctrl+Shift+F".to_string()]
}

pub(crate) fn default_zoom_in() -> Vec<String> {
    vec!["Ctrl+Alt++".to_string(), "Ctrl+Alt+=".to_string()]
}

pub(crate) fn default_zoom_out() -> Vec<String> {
    vec!["Ctrl+Alt+-".to_string(), "Ctrl+Alt+_".to_string()]
}

pub(crate) fn default_reset_zoom() -> Vec<String> {
    vec!["Ctrl+Alt+0".to_string()]
}

pub(crate) fn default_toggle_zoom_lock() -> Vec<String> {
    vec!["Ctrl+Alt+L".to_string()]
}

pub(crate) fn default_refresh_zoom_capture() -> Vec<String> {
    vec!["Ctrl+Alt+R".to_string()]
}
