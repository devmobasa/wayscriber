{
  "schema_version": 1,
  "notes": [
    "Fixture, never loaded by a real run. `minimal-healthy.json` after the",
    "modern channel was appended with Cargo's `-F` alias. The lane span still",
    "matches the baseline lane verbatim, so a validator that recognizes only",
    "the long `--features` spelling accepts a command whose graph no longer",
    "matches the lane's declared v1_4 floor."
  ],
  "driver": "./tools/run-cargo-consumer.py",
  "lanes": {
    "config-baseline": {
      "description": "Configurator at the deb/rpm baseline, libadwaita pinned to the v1_4 floor.",
      "args": [
        "-p",
        "wayscriber-configurator",
        "--no-default-features",
        "--features",
        "tablet-input"
      ],
      "covers_configurator": true,
      "libadwaita_floor": "v1_4"
    }
  },
  "consumers": {
    "fixture-gate": {
      "description": "One clippy operation on the baseline lane.",
      "caller": "fixtures/entry.sh",
      "operations": [
        {
          "lane": "config-baseline",
          "label": "clippy (configurator, baseline)",
          "argv": [
            "cargo",
            "clippy",
            "-p",
            "wayscriber-configurator",
            "--no-default-features",
            "--features",
            "tablet-input",
            "--all-targets",
            "-F",
            "adw-modern",
            "--",
            "-D",
            "warnings"
          ]
        }
      ]
    }
  },
  "entry_points": {
    "fixtures/entry.sh": {
      "description": "Fixture entry point; no file of this name is ever read.",
      "driver_consumers": ["fixture-gate"],
      "loader_consumers": [],
      "removed_commands": []
    }
  },
  "allowed_non_lane_cargo": []
}
