{
  "schema_version": 1,
  "notes": [
    "Fixture, never loaded by a real run. `minimal-healthy.json` with a second",
    "package selected in the `--package=` spelling, past the bare `--`. Both",
    "details are the point: a scan that stops at `--`, or that only compares",
    "whole tokens against `--package`, would accept a command that compiles a",
    "package the lane never names."
  ],
  "driver": "./tools/run-cargo-consumer.py",
  "lanes": {
    "config-baseline": {
      "description": "Configurator at the deb/rpm baseline, libadwaita pinned to the v1_4 floor.",
      "args": [
        "-p",
        "wayscriber-configurator",
        "--no-default-features",
        "--features",
        "tablet-input"
      ],
      "covers_configurator": true,
      "libadwaita_floor": "v1_4"
    }
  },
  "consumers": {
    "fixture-gate": {
      "description": "One clippy operation on the baseline lane.",
      "caller": "fixtures/entry.sh",
      "operations": [
        {
          "lane": "config-baseline",
          "label": "clippy (configurator, baseline)",
          "argv": [
            "cargo",
            "clippy",
            "-p",
            "wayscriber-configurator",
            "--no-default-features",
            "--features",
            "tablet-input",
            "--all-targets",
            "--",
            "--package=wayscriber",
            "-D",
            "warnings"
          ]
        }
      ]
    }
  },
  "entry_points": {
    "fixtures/entry.sh": {
      "description": "Fixture entry point; no file of this name is ever read.",
      "driver_consumers": ["fixture-gate"],
      "loader_consumers": [],
      "removed_commands": []
    }
  },
  "allowed_non_lane_cargo": []
}
